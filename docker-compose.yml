version: '3'

services:
  nest-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hexfinder-api
    ports:
      - 8080:3000
    depends_on:
      - postgres
    env_file:
      - docker.env

  postgres:
    image: postgres:latest
    container_name: postgres
    ports:
      - 5432:5432
    env_file:
      - docker.env
